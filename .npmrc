# electron-builder resolves app dependencies from a flat node_modules tree.
# pnpm's default symlinked layout trips it up, so use a hoisted (npm-like) layout.
node-linker=hoisted

# Let electron / electron-builder run their install scripts (binary downloads).
enable-pre-post-scripts=true
